# Rust Hello World CLI

Run the CLI from the repository root with the standard Rust toolchain:

```bash
cargo run --quiet --manifest-path rust-hello-world/Cargo.toml
```

Expected output:

```text
Hello, world!
```
